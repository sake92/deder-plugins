package example

@main def main(): Unit =
  println("Deder Web Dashboard example")
  println("Run `deder` to start the dashboard.")
  println(s"Then open http://localhost:9292 in your browser.")
  println("Set `enabled = false` in the configuration to stop")
