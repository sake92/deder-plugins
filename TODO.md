

## dashboards
- add more filters to history
- add sort by longest running request etc
- add stats by task type



## Releasing

VERSION=v0.2.0 && git tag -a $VERSION -m "Release: $VERSION"

git push origin $VERSION

